# Copy to .env. Never commit the real thing.
QH_PROVIDER_MODE=mock
# --- live mode only ---
# AWS_REGION=us-east-1
# Sonnet 4.5 is only invocable through a cross-region inference profile -- the
# bare model id returns HTTP 400. Keep the `us.` prefix.
# QH_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250929-v1:0
# QH_SESSION_BUCKET=
# QH_TABLE_NAME=quiet-hours
# QH_MEMORY_ID=
