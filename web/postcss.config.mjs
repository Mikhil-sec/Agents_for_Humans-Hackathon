// Tailwind v4 is a PostCSS plugin. There is no tailwind.config.js: the theme is
// declared in app/globals.css with @theme.
const config = { plugins: { '@tailwindcss/postcss': {} } };
export default config;
